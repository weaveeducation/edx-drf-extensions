""" Django settings for docs compilation.

This should contain the bare minimum necessary for Django to start.
"""

SECRET_KEY = 'insecure-secret-key'

INSTALLED_APPS = (
    'django.contrib.auth',
    'django.contrib.contenttypes',
)
